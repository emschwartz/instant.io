extends layout

block body
  main
    h1 Instant.io - Streaming file transfer over WebTorrent
    p Download/upload files using the <a href="http://webtorrent.io" target="_blank">WebTorrent</a> protocol (BitTorrent over WebRTC). This is a beta.
    h2#logHeading(style='display: none') Log
    .speed
    .log

    h2 ILP Wallet Details
    form(id='ilpDetails')
      p
        label(for='walletAddress') Address:
        input(id='walletAddress' placeholder='alice@red.ilpdemo.org' required)
      p
        label(for='walletPassword') Password:
        input(id='walletPassword' placeholder='alice' required)
      p
        label(for='pricePerGigabyte') Price per gigabyte:
        input(id='pricePerGigabyte' value='0.001')
      p
        button(type='submit') Login

    h2 Start seeding
    p
      | Drag-and-drop a file (or files) to begin sharing. Or choose a file:
      | #{ ' ' }
      input(type='file', name='upload', multiple)

    h2 Start downloading
    form(id='torrentSelector')
      label(for='torrentId') Download from a magnet link or info hash
      input(name='torrentId', placeholder='magnet:' required)
      button(type='submit') Download

    footer
      p: small <a href="http://webtorrent.io" target="_blank">WebTorrent</a> is powered by JavaScript and WebRTC. Works in Chrome, Firefox, and Opera (desktop and Android).
      p: small Code is available on <a href="https://github.com/feross/instant.io" target="_blank">GitHub</a> under MIT License. Run <code>localStorage.debug = '*'</code> in the console and refresh to enable verbose logs.
